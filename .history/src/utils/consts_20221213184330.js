export const ACCESS_TOKEN = "access_token";
export const CURRENT_USER = 'current_user'
const APP_NAME = "shoppingapp";
export const CAPITALIZED_APP =
  APP_NAME[0].toUpperCase() + APP_NAME.slice(1).toLowerCase();
