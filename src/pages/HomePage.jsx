// import { selectCurrentUser } from '../features/auth/authSlice'

function HomePage() {
	// const user = useSelector(selectCurrentUser)
	// console.log(user)
	return <div className="alert-success">Homepage</div>
}

export default HomePage
